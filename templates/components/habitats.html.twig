{% block habitats %}

  {% for habitat in habitats %}
    <section 
      id="section__{{ habitat.name }}"
      class="img--habitat"
      style="background-image: url('assets/images/{{habitat.image}}')"
    >
      <h2>{{ habitat.name }}</h2>
      <button class="bgBtn--{{ habitat.name }}" data-id="{{ habitat.name }}"><a href="{{ path('app_habitat', {'name': habitat.name })}}">Découvrir</a></button>
    </section>

  {% endfor %}
  
{% endblock %}